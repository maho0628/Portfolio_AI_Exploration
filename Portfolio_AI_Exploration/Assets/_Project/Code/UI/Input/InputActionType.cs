using UnityEngine;
/// <summary>
/// 各 Action Map 内で使用する Input Action 名の定義
/// InputActionAsset 内の Action 名と一致する必要がある
/// </summary>
public enum InputActionType
{
    Ultimate,
    Retry,
    BackToTitle,
    Click,
    Navigate,
    Submit,
    Cancel
}